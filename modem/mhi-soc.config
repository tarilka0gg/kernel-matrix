# MHI modems soldered to the board via PCIe (not USB) — typically the Snapdragon
# X series in ARM/some x86 laptops with a SoC modem.
CONFIG_MHI_BUS=m
CONFIG_MHI_WWAN_CTRL=m
CONFIG_MHI_WWAN_MBIM=m
CONFIG_MHI_NET=m
# gate: MHI_WWAN_* live under net/wwan, gated by if WWAN
CONFIG_WWAN=m
