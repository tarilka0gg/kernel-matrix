# Sandy/Ivy Bridge and Jaguar have AVX1 but NOT AVX2 — so the v3 tier (which
# requires AVX2) doesn't fit them; the correct kernel tier here is v2, not v3.
CONFIG_GENERIC_CPU=y
CONFIG_X86_64_VERSION=2
# CONFIG_X86_NATIVE_CPU is not set
# CONFIG_MZEN4 is not set
# AMD touchpad: GPIO controller (IRQ) — built-in, so it doesn't depend on module load order.
CONFIG_PINCTRL_AMD=y
